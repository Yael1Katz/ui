import { ThemeRunDirective } from './theme-run.directive';

describe('ThemeRunDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeRunDirective();
    expect(directive).toBeTruthy();
  });
});
